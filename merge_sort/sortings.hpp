#ifndef SORTINGS_HPP
#define SORTINGS_HPP
#include <vector>
    void Insert_sort(std::vector<int> &array_to_sort, int m);
    void Merge(std::vector<int> &array_to_sort, int left_edge, int right_edge, int m, int median);
    void Merge_sort(std::vector<int> &array_to_sort, int left_edge, int right_edge, int m);

#endif
